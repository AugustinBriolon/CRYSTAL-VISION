import Section from '../shared/sections';

export default function Hero() {
  return (
    <Section className="flex flex-col items-center justify-center bg-white" id="hero">
      <h1>Hero</h1>
      <p>
        This is the hero section This is the hero section This is the hero section This is the hero
        section This is the hero section This is the hero section This is the hero section This is
        the hero section This is the hero section{' '}
      </p>
    </Section>
  );
}
