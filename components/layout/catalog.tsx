import Section from '@/components/shared/sections';

export default function Catalog() {
  return (
    <Section className="flex flex-col items-center justify-center bg-white" id="catalog">
      <h2>Catalog</h2>
      <p>This is the catalog section</p>
    </Section>
  );
}
